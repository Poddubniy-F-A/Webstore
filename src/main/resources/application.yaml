server:
  port: 8080


spring:
  application:
    name: main

  datasource:
    url: jdbc:mysql://localhost:3301/webstoreDB
    username: root
    password: pass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
